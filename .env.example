# Convex
CONVEX_DEPLOYMENT=dev:your-deployment-id # team: your-team, project: film-fanatic
VITE_CONVEX_URL=https://your-project.convex.cloud
VITE_CONVEX_SITE_URL=https://your-project.convex.site

# Clerk Auth
CONVEX_CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev
VITE_CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_CLERK_PUBLISHABLE_KEY

# App URLs
VITE_PUBLIC_APP_URL=https://pebbly.vercel.app

# TMDB API (read-only public API key — safe for client)
VITE_PUBLIC_TMDB_ACCESS_TOKEN=your_tmdb_read_access_token
VITE_PUBLIC_TMDB_API_URL=https://api.themoviedb.org/3

# Convex Deploy
CONVEX_DEPLOY_KEY=prod:your-deployment|your-deploy-key

# AI Providers (server-only — never expose in client code)
OPENROUTER_API_KEY=sk-or-v1-your-key
GEMINI_API_KEY=your_gemini_key

# ---- Cloudflare Workers backend (post-Convex migration) ----
# Local dev secrets live in .dev.vars (gitignored); production uses
# `wrangler secret put <NAME>` with the same names.

# Clerk (server-side session token verification via @clerk/backend)
CLERK_SECRET_KEY=sk_test_YOUR_CLERK_SECRET_KEY
CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev

# Upstash Redis (Drizzle query cache + rate limiting)
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-rest-token
UPSTASH_REDIS_READONLY_REST_TOKEN=your-readonly-rest-token

# Cloudflare (only needed for `pnpm db:studio` remote dashboard + GitHub Actions)
CLOUDFLARE_API_TOKEN=your-api-token-with-d1-edit
CLOUDFLARE_ACCOUNT_ID=your-account-id
